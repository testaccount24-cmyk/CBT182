Note: The following members are in this data set

  $$$$$$$$ -- Documentation vector
  $$$DOC   -- This member
  $$$$READ -- The place to start
  $$$INST  -- PDS installation instructions
  $$$INSX  -- Old PDS installation instructions (SPF and ISPF V1)
  $$$MATCH -- PDSMATCH directory compare output example and PDSMATCH messages
  $$$PARTX -- Test Results for (Partx) filters in HIST, MAP, IF, et al
  $$$PROSE -- PDS documentation - PDS 7.3 (ASA controls, lower-case)
  $$$PROS2 -- PDS documentation - PDS 8.6 (ASA controls, lower-case)
  $$$QWIK  -- Quick install doc
  $$$SECUR -- PDS security exit documentation
  $$$UNDOC -- Documentation for otherwise undocumented program features
  $$$CTBL  -- How to maintain the system command table for PDS
  $$$CUSTM -- How to customize PDS to add function
  $$$CUT   -- CUT and PASTE edit macro documentation
  $$$EAVLV -- Notes on EAV support in PDS                               s
  $$$FAMS  -- IGWFAMS interface in ISPF
  $$$FAQ   -- PDS not so frequently asked questions
  $$$HERC  -- How to generate PDS for a Hercules/MVS 3.8J environment
  $$$ISDA  -- How to use ISDA (Interactive Short Disassembler) with PDS
  $$$ISODT -- Documentation updates when using the ISO alternative date
  $$$PDSE  -- PDSE support in version 8.6
  $$$RCQDT -- Michael Theys Date Calendar dialog installation
  $$$RJUST -- Sample code to right justify a field
  $$$UCB   -- Dynamic UCB and 4-digit device number support doc
  $$$VSAM  -- Steve Kleeves VSAM Information dialog installation
  $$$XSTAT -- Extended ISPF statistics (z/OS 1.11) support doc
  $$$Y2K   -- Year 2000 date support doc
  $$CMDADD -- How to add a new subcommand (e.g., OFFLOAD) to PDS
  $$$$TEST -- Notes about using TSO TEST to debug PDS 8.6
  $$$$IDF  -- Notes about using ASMIDF to debug PDS 8.6 (PDSMAIN)
  IDFCLIST -- Clists to run ASMIDF
  IDFCMDS  -- ASMIDF commands (short summary)
  IDFJCL   -- JCL to assemble/link PDSMAIN and run ASMLANGX
  IDFMAC   -- ASMIDF Rexx macros (PROFILE and others)
  IDFSETUP -- Allocate libraries &SYSUID.DEV.name for ASMIDF
  LANGXLAB -- Rexx to extract CSECTs and DS 0H labels from ASMLANGX
              file and build ASMIDF 'BREAK' statements
  $$$$ZXDC -- z/XDC map and breakpoint commands used to debug PDS 8.6
  $$CPKMAP -- Note about IKJTSOxx AUTHTSF entry required for FDRCPK (Innovation)
  $$DELINK -- Notes on David Noon's DELINKI delinkedit program
  $$DISASM -- Notes on Dick Thorton's DISASM disassembler program
  $$DOCNEW -- Documentation for subcommands added since PDS Version 7.3
  PCXDRK   -- .PCX file of the Doc (Review $$drk xispmode)
  PCXGP    -- .PCX file of the Greg Price award (Review $$gp xispmode)
  $$MBRGEN -- Lionel Dyck PDSE V2 member generations dialog doc
  $$PGLITE -- PGLITE PDSE V2 member generations dialog doc
  $$MXIBAT -- List of known issues with MXI 4.3 and some z/OS 2.3 zaps
  $$PDSABC -- PDS Action Bar Commands
  $$PDSMAN -- Documents changes to support the PDSMAN product
  PPTPDS   -- .PPT file - "PDS Command Processor" presentation
  PPTNEW   -- .PPT file - "PDS - What's New" presentation
  $$STRUCT -- General Register usage for PDS and @source-CSECT-Function table
  ZIPS2214 -- .ZIP file - "PDS Lab - The Swiss Army Knife of Utilities" at SHARE
  ZIPS2837 -- .ZIP file - "PDS - The Swiss Army Knife of Utilities" at SHARE
  $$USAGE  -- Documents changes to the USAGE PDS180I message to
              support CA-ASM2, DMS/OS and FDR/ABR
  $$ZNEXTG -- A request to the zNextGeneration
  ZIPZXPO0 -- .ZIP file - "PDS - The Swiss Army Knife of Utilities" at z/Expo
  $$ZOS108 -- Note about COMPARE (SuperC) problem in z/OS 1.8
  $$ZOS109 -- Note about Program Management Binder Fast Data Access service
  $$ZOS111 -- Note about ISPF extended statistics support in PDS
  $$ZOS113 -- Note about IEBPDSE support in PDS
  $$ZOS201 -- Note about IEC190I OPEN DCBE validation message
  $$ZOS202 -- Note about OEDIT/EDIF APAR's to fix bad directory data
  $$ZOS203 -- Note about 8-Character TSO User IDs and LPD search
  $$ZOS205 -- Note about IEBCOPY support of member generations COPY
  $$ZOS301 -- Note about PDSE directory entries not processed correctly
  $ARMODE  -- AMODE and RMODE validation table
  $COMPARA -- Zap to increase IKJDAIR space allocation for OUTDD file
  $COMPARC -- Zaps to prevent parts of SuperC output from being omitted
              in the COMPARE subcommand
  $CONDEND -- Sample batch clist run with CONDEND to stop execution on ERRORS
  $COPYMOD -- IEBCOPY message that explains why COPYMOD did not re-block member
  $DISKMAP -- Note about IKJTSOxx AUTHTSF entry required for DISKMAP (CBT 792)
  $FILE182 -- Renamed to @FILE182 (CBT standard)
  $LOGWIDE -- Notes on "Output Log" when using the LOGWIDE option
  $DEAV    -- Notes on DEAV and DSLIST outputs
  $LMOD    -- Notes on LMOD outputs
  $DIRFTB1 -- DIRENTRY Large Program Object and Extended Attributes outputs
  $MAINRTN -- Synopsis of PDSMAIN processing routines
  $OY07766 -- APAR to protect the integrity of the DSCB of PDS's
  $OA60639 -- APAR to provide the copy of PDSE Member Generations
  $PDSARTn -- The PDS program, by Sam Golob
  $PDSPAIN -- 1988 PDSPAIN committee report to SHARE
  $PTSEXP  -- Sample compacted SYSMOD
  $TSO8... -- Implementation of 8-Character TSO User ID support
  $S378ABE -- Notes about S378 and S478 abends after exiting PDS
  $UPDATES -- List updates to the PDS command:  1997-2023
  $VTOCEAV -- Notes about using the EAV VTOC command with PDS
  $XMIBUFF -- IKJEFTSR command buffer limit information

  $....... -- Other doc members

  M$ENTER through M$TX are PDS macros.

  @DIALOG  -- PDS ISPMODE, MEMLIST, CAX, LISTA, LISTC and LISTV ISPF commands
  @SUBS    -- PDS subcommand subroutines
  @PARSE   -- PDS parse CSECT
  @PDSMAIN -- PDS mainline code
  @FILE182 -- CBT file 1 (doc) narrative for this distribution
  @...        PDS copy members for @SUBS and @DIALOG

  @PSILSTC -- Simonetti Pietro's version of @DIALSTC for VTOC EAV version

  PDSASMH  -- JCL to install PDS with Assembler H
              (OS/390 2.4 and below)
  PDSJCL   -- JCL to install PDS
  PDSJCLAD -- JCL to install PDS with the High Level Assembler
              and ADATA for source debugging with z/XDC
  PDSJCLHL -- JCL to install PDS with the High Level Assembler
  SMPINST  -- SYSMOD to install PDS
  SMPJOBS  -- Notes on SMP install jobs
  SMPALLOC -- Allocate SMP datasets
  SMPCSI   -- Allocate CSI and add zones
  SMPDDDEF -- Define SMP DDDEF entries
  SMPRECV  -- Receive the PDS command package
  SMPAPPLY -- Apply the PDS command package
  SMPACCPT -- Accept the PDS command package
  SMPUMOD1 -- Usermod to replace TP instruction with CSRUNIC FUNCTION=TP macro
  SMPUMOD2 -- Usermod to add scale line to ISPMODE Log screen
  SMPUMOD3 -- Usermod to add module PDSEGENI to APDPLINK
  PDS$JCL  -- JCL to print $$$PROSE
  PDSBATCH -- JCL to run PDS in batch
  PDSHJCL  -- JCL to assemble, link and execute helplist
  PDSPJCL  -- JCL to copy panels and the message member (as well as
              instructions for deleting the PDS 8.5 ISPF materials)
  PDSJCLUT -- JCL to receive the utility programs that PDS uses

  PDSCOMP  -- Sample execution mode sensitive clist
  PDSEQU   -- TSO TEST equates for WORKAREA DSECT
  PDSEQU2  -- TSO TEST equates for SPWRKC   DSECT
  PDSGRPS  -- PDSGRPS batch job to break up file 182 into groups:
              ASM  CLIST  DOC  DOCX  EXEC  EXITS  HELP  JCL  MSGS  OTHER
              PANELS  PCX  PPT  SMP  TSOTEST  XMIT  ZIP
  PDSHELP  -- PDS help (to be placed in 'SYS1.HELP' after editing)
  ISPFPRIM -- ISPF sample primary options panel
  ISPFUTIL -- ISPF sample utility options panel

  #LSPACE  -- LSPACE expanded data area
  #PDSGEN  -- PDS customization member (modify before assembling PDS)
  #PDSGENU -- PDS customization member (modify before assembling PDS)
  #PDSMODS -- PDS modification history doc (version 2.0 to 8.6)
  #PDSTBL  -- PDS subcommand table (modify if subcommands are renamed)
  @DIATBL  -- Dialog command table (modify if subcommands are renamed)

  #MDSECT  -- PDS main GETMAIN area
  #SPWRKA  -- ISPMODE and MEMLIST area (in #MDSECT)

  P86@CUA  -- PDS primary panel
  P86PN@PX -- Old PDS primary panel (non-CUA)
  PDS@PRIM -- PDS primary panel (alias of P86@CUA)
  P86LO@P  -- PDS o line command primary panel for source members
  P86LL@P  -- PDS o line command primary panel for load members
  P86OP@P  -- PDS o command primary panel
  P86OS@P  -- PDS o command primary panel (sequential data sets)
  P86..... -- PDS panels
  PDSM86   -- PDS messages member

  CUT....  -- CUT edit macro and panels
  PASTE..  -- PASTE edit macro and panels
  EQUAL    -- EQUAL edit macro
  EDITXMIT -- PDSEDIT batch program
  $PDSEDIT -- PDSEDIT documentation
  FAMSXMIT -- Dan Dalby FAMSEXTR and HEXDUMP programs (CBT 452)
  CASIO    -- The CASIO Calculadora rexx exec from Pergentino Arias
  EDITLOG  -- EDITLOG rexx exec is used to edit the ISPMODE Log
              with ISPF
  EDSL     -- EDSL rexx exec to invoke Enhanced Data Set List dialog
  FX       -- FX rexx edit macro to FIND ALL after eXclude ALL, then HIDE eXc
              (companion macro to UNX)
  GLOBAL   -- GLOBAL rexx exec to run subcommand for each file in LISTC
  JC       -- JC rexx edit macro and panel for JOB card
  LBDPXMIT -- Lionel Dyck dynamic point-and-shoot header panels
  LMODXMIT -- Locate executable module in LPA, LINKLIB, LNKLST, or STEPLIB
  MCS      -- MCS rexx edit macro to build MCS statements
  P86CHEAT -- PDS Cheat Sheet selection panel
  PDSCMDS  -- PDSCMDS rexx exec to execute PDS Subcommands
  RCQXMIT  -- Michael Theys Calendar dialog
  PGHXMIT  -- TSO Help members for PDSEGEN and PGLITE dialogs
  DOCPGLQR -- PGLITE Quick Reference in Microsoft Word format (binary)
  REEQUAL  -- REEQUAL edit macro
  PLUG     -- PLUG rexx edit macro to plug (overlay) data in lines
  YALE     -- YALE rexx exec to invoke the Yale COMPARE dialog
  REVIVE   -- REVIVE rexx edit macro (creates a deleted members table for
              ISPF Browse and PDS Restore)
  SHOWCUT. -- SHOWCUT edit macro and panel
  COMXMIT  -- COMPAREC (SuperC) and COMPAREW (Comparex) commands
              in TSO XMIT format (load modules)
  SHAXMIT  -- SHA1SUM, SHA2SUM, SHA224S, SHA3SUM, and SHA5SUM
              in TSO XMIT format (load modules)
  TESTXMIT -- TSO TEST user-written commands (LBPTS, LG, and FIND)
  UTILXMIT -- BLKDISK, COBANAL, COMPARE*, DEAV, DELINKI, DISASM,
              DISKMAP, DSAT, DVOL, HEL, MXI, OFFLOAD, PDSLOAD, PDSMATCH,
              REBUILD, RELEASE, RESOURCE, REVIEW, and VTOC commands
              in TSO XMIT format (load modules)
  VSAMXMIT -- Steve Kleeves VSAM Information dialog (CBT 137)
  Z035XMIT -- PDS, PDS38, PDS86, PDS86I, PDS8638 commands
              in TSO XMIT format (load modules)
  ACFC     -- ACFC rexx edit macro (compile ACF2 access/resource rule)
  ABEHELP  -- ABE TSO help member
  DEAVHELP -- DEAV TSO help member
  LMODHELP -- LMOD TSO help member
  DEAVJCL  -- JCL to assemble and link DEAV
  CABAL    -- Vive la revolution
  CBTFTP   -- CBTFTP rexx example (FTP pop-up dialog to XMIT/FTP CBT)
  CSECTS   -- CSECTS rexx exec to display CSECTs for load module
  EFIND    -- EFIND rexx exec to search each file in the LISTF table
  EFSKEL   -- JCL skeleton for EFIND exec
  ESDFLAGS -- ESD AMODE/RMODE flags (HLASM Object deck output)
  FAMSATTR -- FAMSATTR rexx exec to invoke Dan Dalby FAMSEXTR program
  FMVLM    -- FMVLM rexx exec to invoke IBM File Manager view load module info
  GOVRFLB  -- MVT control block (Don't you like the way this sounds?)
  HELPLIST -- HELPLIST assembler source code
  IDCAMS   -- IDCAMS rexx edit macro (to run IDCAMS commands)
  ISFBROW  -- ISFBROW rexx exec to extract XMIT/IEBCOPY messages from SYSOUT
  LISTFISF -- LISTFISF rexx exec to add APF/LPA/LNK/PARMLIB DSNs to LISTF table
  LISTFMXI -- Same as LISTFISF rexx exec, but uses MXI instead of SDSF
  XH2XHI   -- XH2XHI rexx edit macro to change AH, CH, LH, and MH
              instructions to long displacement format (xHI)
  SH2AHI   -- SH2AHI rexx edit macro to change SH
              instructions to long displacement format (AHI   Rx,-n)
  PACKXMIT -- PACKDS and UNPACKDS execs and panels
              in TSO XMIT format
  PDSAMS   -- PDSAMS rexx exec to run IDCAMS command
  PDSBOOK  -- PDSBOOK rexx exec to create a PDS quick reference booklet
              from the PDSHELP member
  PDSDIR4  -- PDSDIR4 rexx exec to read PDS directory with pattern matching
  PDSELM   -- PDSELM rexx edit macro for EDITLOG to position to the
              last ">subcommand" (EDITLOG LAST) in ISPLOG
  PDSMG    -- PDSMG rexx exec to View/Edit a PDSE V2 member generation
  PDSINFO  -- PDSINFO rexx exec to display DSINFO in a pop-up
  PC       -- PC rexx exec will invoke PDS as an action (line) command
              from a SDSF display of PROC, PARM, APF, LNK, or LPA
              list of datasets.
  PDSMODEL -- PDSMODEL rexx exec to allocate a new dataset that is
              based on the active data set
  PDSMSGS  -- PDSMSGS rexx exec to create a list of PDS messages
              from the PDSHELP member (used by P86OPUX MSG command)
  PGLITE   -- Dynamic area rexx exec for PDSE V2 member generations
  PRTMSGS  -- PRTMSGS rexx exec to print a list of PDS messages
              from the PDSHELP member
  PDSLAB   -- SHARE 2214 - PDS Lab instructions for running at home
  PDSLIBDF -- PDS LIBDEF clist
  DEAV     -- DEAV CP to display LSPACE extended statistics
              (allow for EAV volume format)
  DOALL    -- Lionel Dyck rexx exec to run an Edit macro on every member
  DSK3390  -- 3390 space calculation rexx exec
  PDSPARSE -- PDSPARSE rexx exec to find PARSE workarea variables (#) in source
  TIDYPDF  -- TIDYPDF rexx exec to realign assembler code that was copied
              from a web PDF file and uploaded to mainframe
  UNIQUE   -- UNIQUE rexx edit macro to delete duplicate lines in a member
  PDSQWGEN -- PDSQWGEN rexx edit macro to generate Quickref K (index)
              tags in PDSHELP
  PDSQWLOD -- Loads PDSHELPQ to a MVS/QuickRef User database
  PDSQINST -- Lionel Dyck rexx exec for quick install
  QWIKFIX  -- Lionel Dyck rexx edit macro for quick install
  PDS86X   -- Lionel Dyck PDS execution rexx exec (LIBDEF)
  PDSSETUP -- SHARE 2214 - PDS Lab setup clist
  PDSSTATS  - PDSSTATS rexx exec to ADDSTATS to a member with PDSLOAD
  PDSTI    -- PDSTI rexx exec to run CA-1 tape inquiry from LISTC/LISTF
  PDSTOUCH  - PDSTOUCH rexx exec to ADDSTATS to a member with REPRO
  PDSTRAP  -- PDSTRAP clist for 80-byte wide View Log
  PDSTRAPT -- PDSTRAPT test clist for TRAP function
  PDSTRAPW -- PDSTRAP clist for > 80-byte wide View Log (&LOGWIDE=YES)
  PDSTRIM  -- PDSTRIM rexx example (trim VB LRECL down to largest LRECL)
  PDSVERS  -- PDSVERS rexx exec to save PDS version display setting in profile
  RADMCUST -- Janko custom mods for RACFADM dialog (CBT 417)
  RADMHELP -- RACF Help members in ++MAC compressed format
  RRGEN    -- RRGEN programming facility for System/360
  SAMPM1   -- Message member for dialog clist example
  SAMPPREP -- ISPPREP dialog clist example
  SAMPPRE1 -- ISPPREP dialog panel 1
  SAMPPRE2 -- ISPPREP dialog panel 2
  SCPASMCL -- Assembly dialog clist example
  SCPASML  -- Link dialog clist example
  SCPBATAS -- Batch assembly submit dialog clist example
  SCPBATA1 -- Skeleton JCL statements for SCPBATAS
  SCPDSN   -- Support panel for SCPPREP clist example
  SCPPREP  -- Panel preprocessor panel clist example
  SCPENT1  -- Support panel for dialog clist example
  SCPM01   -- Message member for dialog clist example
  RNS      -- RNS rexx exec used to RENAME SWAP for PDSE (P86LOUX)
  SUPC     -- SUPC rexx exec used in line command panel P86LOUX
  TESTUSER -- TSO HELP for user-written TEST subcommands
  TSOTESTn -- TSO TEST breakpoints/equates used to debug PDS 8.6
              and the PDSEGEN function PDSEGDEL
  TSOTRAP  -- Lionel Dyck TSO trap rexx exec
  UNX      -- Edit macro to show first n lines of each 'x-cluded' block
              (companion macro to FX)
  UXM      -- UXM rexx edit macro to un-XMIT a member with TSO REVIEW
  VISTAMAC -- Vista tn3270 macro:  SELECT member from list with mouse
  XDSECT   -- XDSECT rexx exec to generate TSO TEST EQUATEs for WORKAREA
  XDSECT2  -- XDSECT2 rexx exec to generate TSO TEST EQUATEs for SPWRKC

  SPFPRIM  -- SPF version 0 sample primary options panel
  SPFMSGS  -- SPF version 0 PDS messages member

  VSUBCMD  -- A sample exit (with installation JCL) to check subcommands
  VTSOACF2 -- A sample ACF2 R4.0 exit to check TSO commands -- modified 6/12/85
  VTSOCMD  -- A sample exit (with installation JCL) to check TSO commands
  VTSOPCF  -- A sample PCF II exit to check TSO commands
  VTSORACF -- A sample RACF exit to check TSO commands
  ZAPCTL   -- ZAPCTL rexx edit macro to generate AMASPZAP VER/REP
              control statements from PDS LIST module csect output
  ZTSOHELP -- ZTSOHELP rexx exec to invoke TSO HELP directory dialog

  ........ -- A few old members not mentioned in this doc file

  obsolete -- Members with a size of 1 are deleted, renamed, or
              repackaged members ---> IF : BELOW(2) THEN(NEWML
